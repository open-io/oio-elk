---
# Remember, leave a key empty if there is no value.  None will be a string,
# not a Python "NoneType"
#
# Also remember that all examples have 'disable_action' set to True.  If you
# want to use this action as a template, be sure to set this to False after
# copying it.
actions:
  1:
    action: delete_indices
    description: >-
      Delete indices older than value of environment variable ES_CURATOR_DAYS based on oio- prefixed indices
    options:
      ignore_empty_list: True
      disable_action: False
    filters:
    - filtertype: pattern
      kind: prefix
      value: oio-
    - filtertype: age
      source: field_stats
      field: '@timestamp'
      unit: days
      unit_count: ${ES_CURATOR_DAYS}
      direction: older

    # 2:
    #   action: delete_indices
    #   description: >-
    #     Delete indices when space raise value of environment variable ES_CURATOR_DISK_SPACE (Go), based on oio- prefixed indices.
    #   options:
    #     ignore_empty_list: True
    #     disable_action: False
    #   filters:
    #   - filtertype: pattern
    #     kind: regex
    #     value: oio-*
    #   - filtertype: space
    #     disk_space: ${ES_CURATOR_DISK_SPACE}
    #     use_age: True
    #     source: name
    #     timestring: '%Y.%m.%d'
...
